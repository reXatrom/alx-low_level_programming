#include <stdio.h>

/**
 *main - Entry point
 *
 *Return: Always 0 (Success)
 */

int main(void)
{
	char cl;

	char sl;

	sl = 'a';
	cl = 'A';
	while
		(sl <= 'z')
		{putchar(sl);
			sl++;
		}
	while
		(cl <= 'Z')
		{putchar(cl);
			cl++;
		}
	putchar('\n');
	return (0);
}
