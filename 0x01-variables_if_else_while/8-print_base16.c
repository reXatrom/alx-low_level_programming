#include <stdio.h>

/**
 *main - Entry point
 *
 *Return: Always 0 (Success)
 */

int main(void)
{
	char ls;

	int n;

	ls = 'a';
	while
		(n < 10) {
			putchar(n + '0');
			n++;
		}
	while
		(ls <= 'f') {
			putchar(ls);
			ls++;
		}
	putchar('\n');
	return (0);
}
