#include <stdio.h>

/**
 *main - Entry point
 *
 *Return: Always 0 (Success)
 */

int main(void)
{
	char sl;

	sl = 'z';
	while
		(sl >= 'a') {
			putchar(sl);
			sl--;
		}
	putchar('\n');
	return (0);
}
